[project]
name = "baowulearn"
version = "1.3.1"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "gmssl",
    "tinydb",
    "pyside6",
]

[dependency-groups]
dev = [
    "pyinstaller",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 120

[tool.black]
line-length = 120
